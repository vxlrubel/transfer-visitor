;(function($){
    'use strict';
    const doc = $(document);
    class TransferVisitor{
        constructor(){
            // code here
        }
    }

    doc.ready(()=>{ new TransferVisitor() });
})(jQuery);